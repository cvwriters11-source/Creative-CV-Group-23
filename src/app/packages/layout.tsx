import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "CV Packages",
  description:
    "Take the next step in your career with Creative CV packages. Fresh Graduate R950, Professional R1200, Executive R1500, International Résumé R3000.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
